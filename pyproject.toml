[build-system]
requires = ["uv_build>=0.12,<0.13"]
build-backend = "uv_build"

[project]
name = "cta-pipeline"
version = "0.2.0"
description = "A small CTA-backed ingestion and RAG example"
readme = "README.md"
requires-python = ">=3.13"
license = { file = "LICENSE" }
dependencies = []

[project.scripts]
cta-pipeline = "cta_pipeline.__main__:main"

[tool.uv]
package = true
